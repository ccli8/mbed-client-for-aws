{
    "name": "aws-client",
    "config": {
        "log-with-custom": {
            "help": "Output log with custom log function. On enabled, provide aws_iot_log_printf with the same signature as printf",
            "options": [false, true]
        }
    }
}
