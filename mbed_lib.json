{
    "name": "aws-client",
    "config": {
        "log-retarget": {
            "help": "Retarget log output. On enabled, implement aws_iot_log_printf with the same signature as printf",
            "options": [false, true]
        }
    }
}
